1. Run "k_means.m" which is allowing maximum 5 clusters
2. if more cluster is needed just change the value of k
   in the file namely "k_means.m"
3. Output is generated in a scatter plot where red star(*)
   is denoting the centroids of the clusters
4. The vaiable c in the output is representing the position
   of the generated centroids.  